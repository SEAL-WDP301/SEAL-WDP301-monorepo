/**
 * Authentication provider enumeration.
 * Determines how a user authenticates (local credentials or OAuth).
 */
export enum Provider {
  LOCAL = "LOCAL",
  GOOGLE = "GOOGLE",
  GITHUB = "GITHUB",
}
