import type { LucideIcon } from "lucide-react"; import { Card } from "@/components/ui/card";
export interface ManagementMetric { label: string; value: string | number; detail: string; icon: LucideIcon; }
export function ManagementMetricGrid({ metrics }: { metrics: ManagementMetric[] }) { return <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3 2xl:grid-cols-6">{metrics.map((metric) => { const Icon = metric.icon; return <Card key={metric.label} className="gap-0 p-4"><span className="grid size-9 place-items-center rounded-xl bg-orange-500/10 text-orange-400"><Icon className="size-4" /></span><p className="mt-4 text-xs text-muted-foreground">{metric.label}</p><p className="mt-1 text-2xl font-bold">{metric.value}</p><p className="mt-2 text-[11px] text-muted-foreground">{metric.detail}</p></Card>; })}</div>; }
