apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: hackathon
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3000
      name: http
  selector:
    app: frontend
